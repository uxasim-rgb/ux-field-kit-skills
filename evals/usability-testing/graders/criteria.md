---
type: llm
---

PASS if the reply says tasks should state a goal rather than name interface elements or give the path, and warns that using words visible in the UI inflates success. FAIL if the example tasks name buttons or spell out the steps.
